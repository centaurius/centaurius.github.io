<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Coding | Centaurius Blog]]></title>
  <link href="http://centaurius.github.io/:blog/:categories/coding/atom.xml" rel="self"/>
  <link href="http://centaurius.github.io/"/>
  <updated>2017-02-14T19:39:42+00:00</updated>
  <id>http://centaurius.github.io/</id>
  <author>
    <name><![CDATA[Hamilton Andrew White]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Brython Example]]></title>
    <link href="http://centaurius.github.io/:blog/2017/02/14/brython-example.html"/>
    <updated>2017-02-14T19:34:19+00:00</updated>
    <id>http://centaurius.github.io/:blog/2017/02/14/brython-example</id>
    <content type="html"><![CDATA[<p>&lt;!doctype html>
<html>
<head>
<meta name="description" content="Analog clock demo written in Brython www.brython.info">
<meta name="keywords" content="Python,Brython">
<meta name="author" content="Pierre Quentel">
<meta charset="iso-8859-1"></p>

<script type="text/javascript" src="http://centaurius.github.io/src/brython.js"></script>


<script type="text/javascript" src="http://centaurius.github.io/src/py_VFS.js"></script>


<p></head></p>

<p><body onLoad="brython(1)">
<img src="../brython.png"></p>

<p><p></p>

<p><script type="text/python">
import time
import math
import datetime</p>

<p>from browser import document as doc
import browser.timer</p>

<p>sin,cos = math.sin,math.cos
width,height = 250,250 # canvas dimensions
ray = 100 # clock ray</p>

<p>def needle(angle,r1,r2,color=&ldquo;#000000&rdquo;):
    # draw a needle at specified angle in specified color
    # r1 and r2 are percentages of clock ray
    x1 = width/2-ray<em>cos(angle)</em>r1
    y1 = height/2-ray<em>sin(angle)</em>r1
    x2 = width/2+ray<em>cos(angle)</em>r2
    y2 = height/2+ray<em>sin(angle)</em>r2
    ctx.beginPath()
    ctx.strokeStyle = color
    ctx.moveTo(x1,y1)
    ctx.lineTo(x2,y2)
    ctx.stroke()</p>

<p>def set_clock():
    # erase clock
    ctx.beginPath()
    ctx.fillStyle = &ldquo;#FFF&rdquo;
    ctx.arc(width/2,height/2,ray<em>0.89,0,2</em>math.pi)
    ctx.fill()</p>

<pre><code># redraw hours
show_hours()

# print day
now = datetime.datetime.now()
day = now.day
ctx.font = "bold 14px Arial"
ctx.textAlign = "center"
ctx.textBaseline = "middle"
ctx.fillStyle="#FFF"
ctx.fillText(day,width*0.7,height*0.5)

# draw needles for hour, minute, seconds    
ctx.lineWidth = 3
hour = now.hour%12 + now.minute/60
angle = hour*2*math.pi/12 - math.pi/2
needle(angle,0.05,0.5)
minute = now.minute
angle = minute*2*math.pi/60 - math.pi/2
needle(angle,0.05,0.85)
ctx.lineWidth = 1
second = now.second+now.microsecond/1000000
angle = second*2*math.pi/60 - math.pi/2
needle(angle,0.05,0.85,"#FF0000") # in red
</code></pre>

<p>browser.timer.set_interval(set_clock,100)</p>

<p>canvas = doc[&ldquo;clock&rdquo;]</p>

<h1>draw clock border</h1>

<p>ctx = canvas.getContext(&ldquo;2d&rdquo;)
ctx.beginPath()
ctx.lineWidth = 10
ctx.arc(width/2,height/2,ray,0,2*math.pi)
ctx.stroke()</p>

<p>for i in range(60):
    ctx.lineWidth = 1
    if i%5 == 0:
        ctx.lineWidth = 3
    angle = i<em>2</em>math.pi/60 - math.pi/3
    x1 = width/2+ray<em>cos(angle)
    y1 = height/2+ray</em>sin(angle)
    x2 = width/2+ray<em>cos(angle)</em>0.9
    y2 = height/2+ray<em>sin(angle)</em>0.9
    ctx.beginPath()
    ctx.moveTo(x1,y1)
    ctx.lineTo(x2,y2)
    ctx.stroke()</p>

<p>def show_hours():
    ctx.beginPath()
    ctx.arc(width/2,height/2,ray<em>0.05,0,2</em>math.pi)
    ctx.fillStyle = &ldquo;#000&rdquo;
    ctx.fill()
    for i in range(1,13):
        angle = i<em>math.pi/6-math.pi/2
        x3 = width/2+ray</em>cos(angle)<em>0.75
        y3 = height/2+ray</em>sin(angle)<em>0.75
        ctx.font = &ldquo;20px Arial&rdquo;
        ctx.textAlign = &ldquo;center&rdquo;
        ctx.textBaseline = &ldquo;middle&rdquo;
        ctx.fillText(i,x3,y3)
    # cell for day
    ctx.fillStyle = &ldquo;#000&rdquo;
    ctx.fillRect(width</em>0.65,height<em>0.47,width</em>0.1,height*0.06)</p>

<p>show_hours()
set_clock()
</script></p>

<p><p>
<canvas width="250" height="250" id="clock">
Your browser doesn&rsquo;t support Canvas
</canvas></p>

<p></body>
</html></p>
]]></content>
  </entry>
  
</feed>
